[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    pytest
    coverage
commands =
    coverage run -m pytest tags.py
    coverage-tags xml
    coverage run -m pytest posts.py
    coverage-posts xml

[coverage:run]
relative_files = True
source = project/tests/
branch = True